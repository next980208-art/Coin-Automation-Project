# BTCUSDT USDT-M 프로젝트 실행 이력, 문제 해결 및 기술적 판단 기록

기록 범위: 2026-08-16 ~ 2026-08-25  
대상: Binance BTCUSDT USDT-M 데이터 수집, PyFlink 가공, ML 연구, Kafka 실시간 피처, 페이퍼 안전 경로  
안전 상태: 실제 거래소 주문 API 미연결, API 키 미사용, `no_trade` 전용

---

## 1. 이 문서의 목적

이 문서는 프로젝트를 진행하면서 실제로 발생한 문제와 해결 과정을 시간 흐름으로 정리한 기술
회고록이다. 단순히 “무엇을 만들었다”가 아니라 아래 네 가지를 남기는 것이 목적이다.

1. 어떤 증상이 발생했는가
2. 왜 발생했는가
3. 어떻게 수정했는가
4. 수정 뒤 무엇으로 정상 동작을 확인했는가

따라서 이 문서에서 말하는 **성공**은 데이터 경로, 저장, 검증, 서비스 연결이 정상이라는 뜻이다.
모델 수익성이나 실거래 가능성을 뜻하지 않는다.

---

## 2. 최종적으로 확보한 현재 상태

문제를 해결하며 현재까지 확보한 기반은 아래와 같다.

```text
과거 데이터
Binance REST -> Airflow 14일 청크 -> PyFlink Batch -> Parquet Feature Store
              -> 성공 마커 -> 전체 품질 검사 -> 성공한 원천 CSV 삭제

실시간 데이터
Binance WebSocket -> Kafka 원천 Topic -> PyFlink Streaming -> Kafka 피처 Topic
                  -> no_trade 추론 -> 리스크 거절·감사 로그

연구
Feature Store -> 라벨 -> 시간순 데이터셋 -> XGBoost -> 비용 포함 백테스트
```

2026-08-25 기준 Feature Store 검증 결과:

| 항목 | 결과 |
| --- | ---: |
| Parquet 파일 | 38개 |
| 성공 마커 | 27개 |
| 총 행 수 | 506,880행 |
| 연속 데이터 범위 | 2025-09-04 00:00 ~ 2026-08-21 23:59 UTC |
| 연속 기간 | 352일 |
| timestamp 중복 | 0건 |
| 1분 간격 공백 | 0건 |
| 필수 컬럼 결측 | 0건 |
| 품질 관문 상태 | `healthy: true` |

이 상태는 5년 백필이 모두 끝났다는 뜻은 아니다. Airflow가 PC와 Docker Desktop이 실행된 동안
다음 과거 청크를 자동으로 계속 처리하는 상태다.

---

## 3. 진행 흐름 요약

| 시기 | 진행한 일 | 핵심 결과 |
| --- | --- | --- |
| 2026-08-16 | 7일·2주 OHLCV 시험 수집과 가공 | 파일 수집·Parquet 저장·원천 삭제 순서를 검증했지만, 초기 `2_flink_processor.py`는 실제 Apache Flink가 아닌 로컬 Pandas 처리기였음 |
| 2026-08-19~20 | PyFlink Batch 환경과 데이터·백테스트 코드 감사 | 실제 Flink Job, 스키마 마커, 청크 경계 문맥, 시간 누수·비용 계산 보정 |
| 2026-08-22 | 실제 Binance 1,000건 Kafka -> Flink -> Parquet 시험 | Producer 1,000건, Consumer 1,000건, Flink 저장 1,000건 확인 |
| 2026-08-23 | 실제 90일 Feature Store·선물 맥락·ML·백테스트 | 파이프라인과 2% 상한은 검증, 모델 수익성은 미통과 |
| 2026-08-23 | 선택형 방향 모델 개선 실험 | 피처를 늘려도 성능이 더 나빠져 후보 모델 미승인 유지 |
| 2026-08-25 | Airflow 과거 백필 자동화와 전체 품질 관문 | 성공 마커 기반 재개, stale staging 복구, 352일 연속 데이터 검증 |
| 2026-08-25 | 실제 WebSocket -> Kafka -> PyFlink 전환 | 실시간 피처, `no_trade`, 리스크 감사까지 연결. 주문 API 호출 0건 |

---

## 4. 초기 데이터 수집과 가공 단계

### 4.1 7일 시험: 이름과 실제 기술이 달랐던 문제

#### 진행 내용

초기에는 아래 명령으로 BTC/USDT Spot 1분봉 7일치를 시험했다.

```powershell
python 1_chunk_downloader.py --start-date 2024-01-01 --days 7 --no-kafka
python 2_flink_processor.py
python 3_ml_training.py
```

수집·처리 행 수는 10,080행이었고, Parquet 저장과 성공 표시 파일 생성, 검증 뒤 원천 CSV 삭제를
확인했다.

#### 문제

파일 이름은 `2_flink_processor.py`였지만, 이 초기 처리기는 Apache Flink Cluster에 제출된
Job이 아니라 로컬 Pandas 처리기였다. 또한 Kafka connector를 찾지 못하거나 버전이 맞지 않아
Kafka 기반 가공 SQL은 시작되지 않았다.

#### 왜 문제인가

“Flink로 가공했다”라고 설명하면 실제 Flink Dashboard에서 실행된 Job을 뜻하는 것으로
이해될 수 있다. 로컬 Pandas 검증과 Apache Flink 분산 Job은 실행 방식, 장애 처리, 확장성이
다르다. 가공이 됐는지와 Flink가 실행됐는지를 구분하지 않으면 프로젝트 상태를 잘못 판단한다.

#### 해결

1. 초기 단계에서는 파일 기반 수집·Pandas 검증을 임시 시험으로 명확히 구분했다.
2. 큰 API 응답 한 번에 의존하지 않고 OHLCV를 페이지 단위·날짜 단위로 수집했다.
3. 이후 실제 PyFlink Batch Job인 `flink_jobs/batch_feature_job.py`와 제출 경로를 구축했다.
4. 문서에서도 초기 로컬 처리와 실제 PyFlink 실행을 분리해 기록했다.

#### 검증 결과

2026-08-20에 고정 CSV 6행을 실제 Flink에 제출했고 Job ID
`23600ba505b26342a8e97cac981141cc`가 `FINISHED` 상태가 된 것을 확인했다.

---

### 4.2 2주 청크 시험: 원천 삭제 순서 검증

#### 진행 내용

```powershell
python 1_chunk_downloader.py --start-date 2024-01-01 --days 14 --no-kafka
python 2_flink_processor.py
python 3_ml_training.py
```

기대 행 수와 처리 행 수는 모두 20,160행이었고, Parquet 검증과 성공 표시 파일 생성을 확인했다.

#### 문제 가능성

개인 PC에서 장기간 데이터를 처리할 때 원천 CSV를 바로 삭제하면 저장 실패나 잘못된 피처 계산이
발생했을 때 재처리할 재료가 사라진다. 반대로 5년 원천 CSV를 모두 보관하면 디스크 용량이 빠르게
부족해진다.

#### 해결

아래 순서를 완료 조건으로 고정했다.

```text
원천 CSV 수집
  -> PyFlink 가공
  -> Parquet 다시 읽기
  -> 행 수·스키마 확인
  -> 성공 마커 작성
  -> 원천 CSV 삭제
```

#### 왜 이렇게 했는가

원천을 짧게 보관하면 재처리는 가능하고, 성공한 데이터만 삭제하면 용량도 절약할 수 있다.
성공 마커는 단순히 파일 존재 여부가 아니라 “이 기간은 저장과 검증을 모두 통과했다”는 증거가 된다.

---

## 5. 실제 PyFlink 배치 환경에서 발생한 문제

### 5.1 CSV 헤더가 데이터 행으로 들어간 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | Flink가 CSV 첫 줄의 컬럼 이름을 가격·시간 데이터처럼 읽을 수 있었음 |
| 원인 | 고정 스키마 입력인데 헤더가 있는 CSV를 전달 |
| 해결 | Flink 입력용 원천은 헤더 없는 고정 스키마 CSV로 통일 |
| 이유 | 데이터 행과 메타데이터를 분리해야 형 변환 오류와 행 수 불일치를 막을 수 있음 |
| 확인 | 고정 데이터 6행 Flink Job이 `FINISHED`, 결과 Parquet 검증 통과 |

### 5.2 Docker bind mount staging 쓰기 권한 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | TaskManager가 Windows 공유 폴더의 Flink staging에 쓰지 못해 Job 저장이 실패 |
| 원인 | Windows bind mount와 Linux 컨테이너 사용자 권한 차이 |
| 해결 | Flink 임시 staging을 Docker named volume `flink_staging`으로 분리 |
| 이유 | 컨테이너 내부 권한과 파일 잠금 문제를 줄이고, 실행 중 임시 파일을 안정적으로 관리하기 위해서 |
| 확인 | 실제 PyFlink Batch Job이 Parquet와 성공 마커를 생성 |

### 5.3 Airflow·TaskManager 간 권한 불일치

| 항목 | 내용 |
| --- | --- |
| 증상 | Flink가 만든 파일을 Airflow가 마지막 정리 단계에서 삭제하지 못함 |
| 원인 | 서로 다른 컨테이너 사용자와 root 소유 파일 |
| 해결 | TaskManager를 `umask 000`으로 시작하고 staging 초기화 시 쓰기 권한을 부여 |
| 이유 | 가공 성공 뒤 정리 실패가 Airflow 전체 실패로 보이는 상황을 막기 위해서 |
| 확인 | Parquet 저장, 성공 마커, 원천 CSV 삭제, staging 정리가 모두 완료 |

### 5.4 출력 파일 확장자에 의존한 검증 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | Flink 출력 part 파일에 `.csv` 확장자가 없어 검증기가 결과를 못 찾을 수 있었음 |
| 원인 | Flink 출력 파일 이름이 일반 CSV 파일명 규칙과 다름 |
| 해결 | 검증기가 확장자 대신 `part-*` 출력 파일을 읽도록 변경 |
| 이유 | 처리 엔진이 만든 실제 파일 구조를 기준으로 검증해야 하기 때문 |
| 확인 | Flink 출력 Parquet/part 파일 검증 통과 |

### 5.5 백필 실행 작업 폴더 오류와 불필요한 Kafka 의존성

2026-08-23 실제 2일 v2 배치 실행에서 두 가지 문제가 추가로 발생했다.

| 증상 | 원인 | 해결 | 이유 |
| --- | --- | --- | --- |
| Airflow 컨테이너가 하위 수집기를 찾지 못함 | 실행 작업 폴더가 프로젝트 루트가 아니었음 | 백필 실행의 working directory를 프로젝트 루트로 고정 | 상대 경로 기반 하위 명령이 항상 같은 위치에서 실행되게 함 |
| 배치 경로에서 OpenSSL 의존성 충돌 | 실제로 쓰지 않는 Kafka Producer 모듈을 미리 import | Kafka Producer를 실제 Kafka 전송 시점에만 지연 import | 과거 REST 배치와 Kafka 실시간 의존성을 분리해 불필요한 장애를 제거 |

수정 뒤 실제 Binance USDT-M 2일치 2,880행을 하루 청크 두 개로 PyFlink 처리했고, 중복과
핵심 피처 결측 없이 저장했다.

---

## 6. 데이터 정확성과 백테스트 신뢰성을 위해 수정한 문제

### 6.1 과거 처리기와 마커를 혼동할 수 있었던 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | Pandas 또는 구버전 처리 결과의 마커를 현재 PyFlink Feature Store 결과로 오인할 가능성 |
| 해결 | `feature_store_v2`를 분리하고 성공 마커에 처리기와 피처 스키마 버전을 기록·검증 |
| 왜 필요한가 | 같은 파일명이라도 어떤 코드와 어떤 피처 정의로 만든 데이터인지 구분해야 재현 가능 |

현재 PyFlink 배치 결과의 핵심 기준은 다음과 같다.

```text
processor = apache_flink_pyflink_batch
feature_schema_version = ohlcv_basic_v2_boundary4
```

### 6.2 청크 경계에서 피처 문맥이 끊긴 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | 새 청크 첫 부분에서 `return_1m`이 0으로 계산되거나 이동평균 문맥이 부족할 수 있음 |
| 원인 | 14일 청크를 독립 파일처럼 계산해 직전 봉을 모름 |
| 해결 | 뒤 청크를 계산할 때 직전 연속 4개 봉을 계산 문맥으로 함께 사용 |
| 왜 필요한가 | 이동평균과 직전 수익률은 과거 봉이 있어야 정상 계산되므로, 청크 경계가 가짜 시장 변화를 만들면 안 됨 |
| 확인 | 90일 실행에서 후속 청크 경계 문맥 `[4, 4, 4, 4, 4, 4]`, 핵심 피처 결측 0건 |

### 6.3 미래 정보 누수와 낙관적인 진입 가정

| 문제 | 왜 잘못인가 | 해결 | 검증 |
| --- | --- | --- |
| 완전하지 않은 미래 라벨 사용 가능성 | 라벨 계산에 필요한 미래 시간이 끝나지 않은 행이 들어가면 정답을 미리 알게 됨 | 완전한 라벨 구간만 사용 | 마지막 미래 구간을 데이터셋에서 제외 |
| Train/Test 경계 누수 | 테스트 시작 시점의 미래 가격이 학습 라벨에 들어갈 수 있음 | 분할 경계에서 240분 purge | 시간순 학습·테스트 분리 확인 |
| 같은 봉 종가 진입 | 종가를 본 뒤 바로 그 종가에 들어간 것으로 가정하면 실제보다 유리함 | 다음 봉 시가 진입으로 변경하고 신호 시각과 진입 시각 분리 | 단위 시험에서 1봉 이동 확인 |

### 6.4 포지션 중복과 비용 포함 2% 손실 한도

| 문제 | 증상 | 해결 | 이유 |
| --- | --- | --- |
| 포지션 중복 | 이전 포지션이 끝나기 전에 신규 신호를 받아 구형 백테스트에서 거래 구간이 겹침 | 포지션 종료 전 신규 진입 금지 | 같은 자본을 동시에 여러 번 사용한 것처럼 보이는 오류 방지 |
| 비용을 빼면 손실 2% 초과 가능 | 스탑 손실만 2%로 맞추면 수수료·슬리피지 추가 뒤 실제 손실이 더 커짐 | 스탑·비용·추가 슬리피지를 합쳐 계좌 2%가 되도록 명목금액 정규화 | 사용자 위험 정책을 계산상으로도 지키기 위해서 |

보정 뒤 백테스트에서 거래 1회 최대 실현 손실은 계좌 기준 `-2.00%`로 제한됐다. 단, 이것은
과거 데이터에 가정한 시뮬레이션 결과이며 실거래의 급격한 가격 변동이나 주문 미체결을 보장하지는 않는다.

### 6.5 API 데이터 완전성과 결측값 왜곡

| 문제 | 위험 | 해결 |
| --- | --- | --- |
| OHLCV 또는 mark price 일부만 수집 | 빠진 분이 있어도 저장되면 모델이 거짓 패턴을 학습 | 기대 timestamp와 실제 timestamp가 완전히 맞을 때만 저장 |
| funding 변환 실패를 0으로 처리 | 실제 중립 funding과 오류를 구분할 수 없음 | 변환 실패는 결측값으로 보존 |
| aggTrade 페이지 경계 | 같은 밀리초 체결을 시간에 1ms를 더해 넘기면 일부 체결 누락 가능 | 첫 요청 뒤 집계 체결 `fromId`로 페이지를 연결하고 ID 중복 제거 |

### 6.6 공개 API 한계: 없는 데이터를 0으로 만들지 않음

과거 open interest는 요청 시점·API 제한 때문에 오래된 날짜를 안정적으로 받을 수 없었다.
또한 완전한 5년치 호가창과 전체 체결 원본도 공개 REST API만으로 되돌리기 어렵다.

해결은 가짜 값으로 채우는 것이 아니었다.

```text
open interest 미수집 -> 0으로 변환하지 않음 -> unavailable 또는 결측으로 보존
과거 호가·체결 미시구조 부족 -> 지금부터 WebSocket으로 별도 축적
```

이유는 0이라는 숫자는 실제로 포지션이 없었다는 경제적 의미를 갖기 때문이다. API 오류나
지원하지 않는 과거 범위를 0으로 바꾸면 모델이 잘못된 시장 상태를 학습한다.

---

## 7. 실제 데이터와 모델 연구에서 발생한 문제

### 7.1 90일 실제 데이터 파이프라인 실행

2026-08-23 실제 Binance USDT-M BTCUSDT 1분봉 90일을 14일 청크 7개로 처리했다.

| 항목 | 결과 |
| --- | ---: |
| Feature Store 행 | 129,600 |
| 선물 문맥 행 | 129,600 |
| funding 값 행 | 129,600 |
| open interest 값 행 | 0 |
| 완전한 라벨·ML 데이터셋 | 129,360 |
| 학습 행 / 테스트 행 | 103,248 / 25,872 |
| train/test 경계 purge | 240행 |
| 모델 정확도 | 41.64% |
| 다수 클래스 기준 정확도 | 42.72% |
| 백테스트 거래 수 | 69 |
| 백테스트 수익률 | -28.48% |
| 최대 낙폭 | -29.85% |
| 거래 1회 최대 실현 손실 | -2.00% |

### 7.2 PC 종료로 작업이 중단된 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | 90일 처리 중 PC가 종료되어 7개 청크 중 앞 5개까지만 성공 |
| 원인 | 개인 PC 환경에서는 전원·Docker 중단이 발생할 수 있음 |
| 해결 | 이미 완료된 5개는 성공 마커를 재사용하고, 성공 마커가 없는 다음 청크의 staging만 초기화한 뒤 재개 |
| 왜 이 해결을 택했는가 | 성공한 데이터를 다시 받거나 덮어쓰지 않고, 실패한 부분만 복구해야 시간과 API 호출을 줄일 수 있음 |
| 확인 | 90일 Feature Store 129,600행, 중복·피처 결측 0건 |

이 경험이 이후 Airflow 자동 백필에서 성공 마커 연속성 검사와 stale staging 자동 복구를 넣은
직접적인 이유가 됐다.

### 7.3 연속 손실 제한이 다음 날에도 풀리지 않은 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | 백테스트에서 연속 손실 제한이 한 번 걸린 뒤 UTC 날짜가 바뀌어도 이후 신규 거래를 계속 막음 |
| 원인 | 연속 손실 상태를 날짜와 분리해 하나의 전역 상태로 유지 |
| 해결 | 연속 손실 상태를 UTC 날짜별로 초기화·관리 |
| 이유 | “하루 단위 손실 중단” 규칙은 다음 날 새 거래까지 영구 차단하는 규칙이 아니기 때문 |
| 확인 | 수정 후 모델 학습과 백테스트를 다시 실행해 최종 수치를 기록 |

### 7.4 모델 수익성이 미통과인 문제

이것은 코드 오류가 아니라 중요한 연구 결과다. 모델 정확도가 다수 클래스 기준보다 1.07%p 낮고,
비용 포함 백테스트 수익률이 음수였다.

#### 선택한 조치

```text
후보 모델을 paper_approved로 등록하지 않음
-> 실시간 추론 서비스는 no_trade만 발행
-> 리스크 서비스는 거절 사유와 감사 로그만 기록
-> 실제 주문 API는 계속 차단
```

#### 왜 이렇게 했는가

데이터 파이프라인이 통과했다고 해서 전략이 수익성이 있는 것은 아니다. 성능 미달 모델을
페이퍼 long/short나 실거래로 넘기면 “검증을 통과하지 않은 전략을 실행”하는 것이 된다.

### 7.5 선택형 방향 모델 개선 실험도 미통과

기존 모델 개선을 위해 수익률, 변동성, 이동평균 이격, 캔들, 상대 거래량, RSI, 시간 주기 등
33개 인과 피처를 추가했다. 피처가 미래 데이터를 쓰지 않는지 여러 시점에서 잘라 인과성 검사도
통과했다.

그러나 결과는 개선되지 않았다.

| 항목 | 기존 v2 | 선택형 v3 |
| --- | ---: | ---: |
| 거래 수 | 69 | 74 |
| 방향 정확도 또는 테스트 정확도 | 41.64% | 48.78% |
| 비교 기준 | 다수 클래스 42.72% | 다수 방향 54.73% |
| 수익률 | -28.48% | -34.64% |
| 최대 낙폭 | -29.85% | -34.64% |
| 거래 1회 최대 손실 | -2.00% | -2.00% |

해결은 “더 높은 확률 기준을 임의로 골라 보기”가 아니었다. 검증 구간에서만 후보 기준을 선택하고
마지막 평가 구간은 한 번만 사용했다. 모든 후보가 검증 구간에서 손실이었으므로 모델을 승인하지
않았다.

---

## 8. Airflow 자동 과거 백필에서 발생한 문제

### 8.1 Binance 심볼 표기 불일치

| 항목 | 내용 |
| --- | --- |
| 증상 | 최초 Airflow Trigger가 `BTCUSDT` 심볼로 수집기를 호출해 실패 |
| 원인 | 수집기는 `BTC/USDT` 표기를 요구하지만, 파일명·마커에는 구분자 없는 `BTCUSDT`가 필요 |
| 해결 | DAG에서 API 수집용 심볼과 성공 마커·파일명용 심볼 코드를 분리 |
| 이유 | 외부 API 계약과 내부 파일 식별자 규칙은 목적이 다르므로 한 문자열로 강제하면 안 됨 |
| 확인 | 수정 뒤 2026-06-11 ~ 2026-06-25, 20,160행 PyFlink 처리와 DAG `success` 확인 |

### 8.2 컨테이너 공유 폴더 권한

| 항목 | 내용 |
| --- | --- |
| 증상 | root 사용자가 만든 `temp_raw_data_v2`와 `feature_store_v2`에 Airflow 사용자가 쓰지 못해 저장 실패 |
| 원인 | Docker 컨테이너 사용자와 호스트 공유 폴더 소유권 불일치 |
| 해결 | 새 임시 폴더 `temp_raw_data_v3` 사용, 기존 저장소·임시 폴더 권한 정리 |
| 이유 | 실패한 원천을 보존하면서도 다음 Airflow 실행이 파일을 쓸 수 있어야 하기 때문 |

### 8.3 실시간 Job과 백필 Job의 슬롯 경쟁

| 항목 | 내용 |
| --- | --- |
| 증상 | TaskManager 슬롯이 1개여서 실시간 Job이 실행 중이면 배치 Job이 제출 후 대기 |
| 원인 | 하나의 TaskManager가 실시간 스트리밍과 유한 배치 작업을 같은 1개 슬롯으로 처리 |
| 해결 | `taskmanager.numberOfTaskSlots`를 2로 변경 |
| 이유 | 한 슬롯은 계속 실행되는 실시간 Job에, 다른 슬롯은 종료되는 백필 Job에 할당하기 위해서 |
| 확인 | 실시간 Job과 Airflow 백필 Job이 함께 처리되는 구성으로 전환 |

### 8.4 컨테이너 중단 뒤 staging과 중간 공백

| 항목 | 내용 |
| --- | --- |
| 증상 | 컨테이너 재생성 중 첫 14일 청크만 성공하고 다음 청크의 원천 CSV와 Flink staging이 남음 |
| 위험 | 가장 오래된 성공 마커만 보고 더 과거로 진행하면, 중간 실패 구간을 영구히 건너뛸 수 있음 |
| 해결 | 최신 데이터부터 연속으로 이어지는 성공 마커만 따라가 다음 범위를 결정. 모든 백필에 `--recover-stale-staging` 전달 |
| 이유 | 과거 방향 백필에서는 “성공 마커가 하나라도 있다”보다 “최신부터 공백 없이 연결됐다”가 더 중요함 |
| 확인 | 2026-04-30 ~ 2026-06-11 공백을 복구하고 전체 1분 간격 누락 0건 확인 |

### 8.5 품질 검사가 없으면 다운로드 성공을 신뢰할 수 없는 문제

#### 해결

`scripts/verify_feature_store.py`를 만들고 Airflow DAG 마지막 Task로 연결했다.

```text
plan_next_backfill
  -> run_next_backfill
  -> verify_feature_store
```

#### 검사 내용

- 모든 Parquet 파일을 실제로 읽을 수 있는지
- 필수 컬럼·스키마가 맞는지
- 핵심 컬럼 결측이 없는지
- timestamp 중복과 1분 공백이 없는지
- 성공 마커가 연속인지
- 마커 행 수·기간과 실제 Parquet 행 수가 일치하는지

#### 왜 이렇게 했는가

다운로드 명령이 끝났다는 사실만으로 데이터가 모델 학습에 적합하다는 보장은 없다. 품질 검사도
Airflow 성공 조건에 넣어야 “정상 저장된 데이터만 다음 단계에서 사용”할 수 있다.

---

## 9. Kafka·실시간 경로에서 발생한 문제

### 9.1 Kafka 시작 순서 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | Kafka가 준비되기 전에 수집기와 소비자가 접속해 `KafkaTimeoutError` 발생 |
| 원인 | Docker `depends_on`만으로 Kafka 내부 준비 완료까지 보장하지 않음 |
| 해결 | 수집기의 Topic 생성에 최대 30회 재시도 추가. 피처·추론·리스크 서비스는 5초 간격으로 Kafka 연결 재시도 |
| 이유 | 컨테이너가 시작됐다는 상태와 메시지 브로커가 요청을 받을 수 있다는 상태는 다르기 때문 |

### 9.2 단일 Kafka 브로커의 내부 Topic 복제 수 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | Consumer Group Coordinator가 동작하지 않음 |
| 원인 | 기본 Kafka 설정은 내부 offsets/transaction Topic의 복제 수를 3으로 기대하지만 로컬 브로커는 1개 |
| 해결 | `KAFKA_BROKER_ID=1` 고정, offsets와 transaction 내부 Topic 복제 수를 1로 지정 |
| 이유 | 개인 PC의 단일 브로커 환경에 맞춘 개발 설정이 필요하기 때문 |
| 주의 | 운영 환경에서 브로커 1개와 복제 수 1은 장애 내성이 낮으므로 그대로 쓰지 않음 |

### 9.3 aggTrade 스트림이 체결 이벤트를 주지 않은 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | `aggTrade`는 체결 이벤트가 오지 않고 bookTicker·depth만 도착해 1분 OHLCV 피처를 만들 수 없음 |
| 원인 | 해당 실행 환경의 aggTrade 스트림 동작이 기대와 달랐음 |
| 해결 | 수집기를 Binance `trade` 스트림으로 전환 |
| 이유 | `trade`에는 가격, 수량, buyer_is_maker 정보가 있어 현재 1분 OHLCV와 taker 불균형 계산에 필요한 입력을 제공 |
| 확인 | 약 139초 동안 trade 5,668건, bookTicker 62,470건, depth 1,362건 수집 |

### 9.4 Flink Kafka 커넥터 버전 호환 문제

| 항목 | 내용 |
| --- | --- |
| 증상 | 기존 Flink 2.3 환경에 현재 PyFlink DataStream 코드와 맞는 Kafka 커넥터를 설치할 수 없음 |
| 해결 | `Dockerfile.flink`, `Dockerfile.airflow`, `docker-compose.yml`, `requirements.txt`를 Flink/PyFlink 2.2.0으로 통일 |
| 이유 | Flink Python API, Kafka 커넥터, Java 의존성은 같은 호환 계열이어야 Job이 실행됨 |

### 9.5 Kafka client 클래스 누락

| 항목 | 내용 |
| --- | --- |
| 증상 | Job 제출 시 `OffsetResetStrategy` 클래스를 찾지 못함 |
| 원인 | Kafka connector JAR가 Kafka client를 포함하지 않는 thin JAR |
| 해결 | `flink-connector-kafka 5.0.0-2.2`와 `kafka-clients 4.2.0`을 함께 설치하고 classpath에 추가 |
| 이유 | 커넥터만으로는 Kafka 프로토콜 클라이언트 클래스가 완성되지 않기 때문 |

### 9.6 Kafka Sink 직렬화와 JSON 이중 인코딩

| 문제 | 증상 | 해결 | 이유 |
| --- | --- | --- | --- |
| 출력 타입 불일치 | PyFlink가 바이트 배열을 내보내는데 Sink가 문자열 직렬화를 시도해 Job 재시작 | JSON 문자열을 UTF-8 bytes로 바꾸고 `ByteArraySchema` 사용 | Kafka에 저장하는 실제 payload 타입과 Sink schema를 맞춤 |
| JSON 이중 인코딩 | Kafka 메시지가 JSON 객체가 아니라 JSON 문자열 안의 문자열이 됨 | 이미 만든 JSON 문자열에는 `json.dumps()`를 다시 하지 않음 | Consumer가 기대하는 객체 계약을 유지 |

이전의 잘못된 테스트 메시지와 새 메시지를 구분하기 위해 추론·리스크 consumer group도 `v3`로
교체했다.

### 9.7 실제 실시간 경로 검증 결과

최종 PyFlink Job `realtime-kafka-feature-job-paper-only`를 제출해 아래 경로를 확인했다.

```text
WebSocket -> Kafka -> PyFlink -> market.features.v1 -> no_trade -> 리스크 감사
```

| 항목 | 결과 |
| --- | ---: |
| Kafka 원천 이벤트 전송 | 69,500건 |
| PyFlink가 새로 만든 1분 피처 | 2건 |
| 추론 서비스 수신 피처 | 2건 |
| 생성 신호 | 2건, 모두 `no_trade` |
| 리스크 감사 | 2건, `no_paper_approved_model` 거절 |
| 거래소 주문 API 호출 | 0건 |

이 결과는 실시간 데이터 처리와 안전한 차단이 동작한다는 증거다. 승인 모델의 long/short 신호,
가상 체결 성과, 실제 주문은 검증하지 않았다.

---

## 10. 문서와 아키텍처 설명에서 수정한 문제

프로젝트가 커지면서 코드 상태와 문서 표현이 어긋날 위험도 발생했다. 아래는 실제 상태를 과장하지
않기 위해 수정한 설명 원칙이다.

| 혼동 가능 표현 | 수정한 설명 | 이유 |
| --- | --- | --- |
| “페이퍼 주문·체결 완료” | 현재는 `no_trade` 거절·감사 경로만 검증. long/short 주문 계획·가상 체결 장기 운용은 미검증 | 코드가 미래 주문 계획을 계산할 수 있는 것과 실제 장기 체결 검증은 다름 |
| “Kill Switch와 일일 중단 구현 완료” | 현재 리스크 코드에는 2% 위험·증거금 계산이 있으며 Kill Switch·일일 중단은 예정 | 문서가 코드보다 앞서가면 안전 상태를 과장하게 됨 |
| “과거와 실시간 피처가 동일” | 과거는 기본 OHLCV 피처 중심, 실시간은 체결·호가 피처 추가. 스키마 정렬 전 직접 결합 금지 | 학습 때 없는 컬럼을 실시간에서만 쓰거나 반대로 쓰는 피처 불일치를 막기 위함 |
| “초록색은 수익성 검증” | 초록색은 코드·실행 경로 확인 표시 | 데이터 처리 성공과 투자 전략 성공은 별개 |
| “Flink라는 파일명이므로 Flink 실행” | 초기 Pandas 처리와 실제 PyFlink Job을 명확히 구분 | 기술 스택과 검증 범위를 정확히 설명하기 위함 |

이 수정은 단순 문서 편집이 아니라, 프로젝트를 발표하거나 GitHub에서 평가받을 때 실제 구현 범위를
정확히 전달하기 위한 안전장치다.

---

## 11. 아직 해결되지 않았거나 다음 단계로 남은 문제

| 남은 항목 | 현재 이유 | 다음 해결 방향 |
| --- | --- | --- |
| 5년 OHLCV 완성 | 현재 352일만 연속 검증됨 | Airflow 자동 백필을 계속 운영하고 품질 보고서 확인 |
| 5년 선물 맥락 결합 | funding·mark·OI를 5년 Feature Store와 동일한 자동 품질 경로로 아직 결합하지 않음 | 수집·시간 정렬·마커·품질 관문을 기존 OHLCV 백필에 추가 |
| 과거 open interest 부족 | 공개 API가 오래된 시점을 안정적으로 제공하지 않음 | 결측을 보존하고, 사용 가능한 범위만 별도 피처로 처리 |
| 5년 과거 호가창·전체 체결 부족 | 공개 REST로 완전 복원 어려움 | WebSocket을 계속 수집해 미래 구간을 별도 미시구조 데이터셋으로 축적 |
| 오프라인·실시간 피처 불일치 | 실시간에 호가·체결 피처가 더 많음 | 공통 피처 모델 또는 별도 미시구조 모델로 분리 |
| 모델 성능 미달 | 90일 v2와 선택형 v3 모두 비용 후 수익률 음수 | 라벨, 보유 시간, 시장 국면, 피처를 시간순 검증으로 재설계 |
| 장기 long/short 페이퍼 | 승인 모델이 없음 | 통과 후보만 버전 고정 후 장기간 관찰 |
| 실제 주문 관리 | stop, 부분 체결, 재시도, Kill Switch 미구현 | 페이퍼 검증 후 별도 주문 시스템으로 구현·검증 |

---

## 12. 이 과정에서 얻은 핵심 교훈

1. **다운로드 성공은 데이터 품질 성공이 아니다.**
   - 저장 뒤 전체 파일·행 수·공백·중복을 검사해야 한다.

2. **처리 엔진 이름보다 실제 실행 증거가 중요하다.**
   - Pandas 처리와 PyFlink Job은 구분해야 하며, Job ID·마커·결과 파일이 증거가 된다.

3. **중단을 전제로 설계해야 한다.**
   - 개인 PC, Docker, 네트워크는 중단될 수 있으므로 성공 마커, stale staging 복구, 재시도는
     부가 기능이 아니라 기본 기능이다.

4. **시간 누수는 모델 성능을 가장 쉽게 과장한다.**
   - 다음 봉 진입, 미래 라벨 제거, 시간순 분할, purge를 적용해야 한다.

5. **손실 한도는 모델 성능과 별개다.**
   - 2% 계산은 모델이 좋아서가 아니라 모델이 틀려도 계좌 위험을 제한하기 위한 규칙이다.

6. **수익이 나지 않는 모델을 승인하지 않는 것도 성공이다.**
   - 현재 `no_trade`는 기능 미완성이 아니라 성능 미달 모델을 주문 경로에서 막은 의도적 결과다.

7. **없는 과거 데이터를 억지로 채우면 안 된다.**
   - open interest 미제공이나 호가창 복원 불가를 0으로 바꾸지 않고 결측·제약으로 보존해야 한다.

8. **문서는 코드 상태와 같이 관리해야 한다.**
   - “구축됨”, “부분 구축”, “계획됨”, “차단됨”을 구분해야 발표와 운영 판단이 정확해진다.

---

## 13. 재현과 확인을 위한 주요 명령

### 전체 기반 서비스 시작

```powershell
PowerShell -ExecutionPolicy Bypass -File .\scripts\start_full_architecture.ps1 -Build
```

### 실제 PyFlink Kafka Job 전환

```powershell
PowerShell -ExecutionPolicy Bypass -File .\scripts\install_flink_kafka_connector.ps1
PowerShell -ExecutionPolicy Bypass -File .\scripts\submit_realtime_flink_job.ps1
```

### Airflow 과거 백필 수동 Trigger

```powershell
docker compose exec -T airflow airflow dags trigger btcusdt_usdm_historical_backfill
```

### Feature Store 전체 품질 검사

```powershell
python scripts\verify_feature_store.py `
  --feature-folder feature_store_v2 `
  --symbol BTCUSDT `
  --market usdm `
  --timeframe 1m
```

### 실시간과 Flink 상태 확인

```powershell
docker compose exec -T jobmanager flink list
Get-Content .\realtime_runtime\inference_metrics.json
Get-Content .\realtime_runtime\risk_audit.jsonl -Tail 5
```

화면 주소:

```text
Airflow: http://localhost:8080
Flink:   http://localhost:8081
```

---

## 14. GitHub에 올릴 때의 주의 사항

이 문서는 코드와 함께 GitHub에 올려도 된다. 하지만 아래 항목은 제외한다.

- 대용량 원천 CSV
- 전체 Parquet Feature Store
- 자동 생성된 대용량 `runtime_reports`
- Kafka/Flink connector JAR
- API 키와 거래소 계정 정보
- 실제 주문 권한을 가진 설정 파일

GitHub에서 이 문서를 읽는 사람은 “문제가 없었다”가 아니라, 문제를 발견하고 왜 위험한지
판단한 뒤 어떻게 검증 가능한 방식으로 고쳤는지를 확인할 수 있다.
